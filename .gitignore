# macOS
.DS_Store
.AppleDouble
.LSOverride

# PDFs (local use only)
*.pdf

# Personal project-level overrides (private)
CLAUDE.local.md

# Personal session data and local settings
.claude/settings.local.json
.claude/.session_*